export PYTHONPATH=$PYTHONPATH:`pwd`
export PYTHONDONTWRITEBYTECODE=1

alias "fastapi:migration:generate"="alembic revision --autogenerate -m"
alias "fastapi:migration:run"="alembic upgrade head"
alias "fastapi:run"="python -m src.main"